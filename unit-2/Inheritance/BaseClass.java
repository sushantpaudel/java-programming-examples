class BaseClass {
  public int value = 10;
  
  public BaseClass(int value) {
    // this.value --> the value in the object
    // value --> the formal parameter passed to the constructor, we put the name same for consistency in code
    this.value = value;
  }
}